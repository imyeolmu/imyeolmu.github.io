### 내부 클래스###

```java
package day01;
/*한 파일내에 여러 클래스를 구성할 경우 public 지정자(제어자)에 한 클래스에만 붙이고 파일명은
public이 붙는 클래스명으로 한다
public은 main()메소드가 있는 클래스에 붙는다
*/

class Demo {
    static int x = 50;
    int y = 60;
    
    public Demo() {

    }

    public void foo(){
            System.out.println("foo()....");
    }

    public static String bar(){
         return "하이 방가방가~~";
    }

}

public class Hello {
    static String str1 = "안녕";
    String str2 = "하이";
    String str3;
//클래스 선언문의 변수 선언
// str1의 클래스 변수
    public static void main(String args[]){
        System.out.println("str1:" + Hello.str1) ;
//str2의 인스턴스 변수
    Hello H = new Hello();   
    System.out.println("str2:" + H.str2);

//Demo클래스의 변수 선언
    System.out.println("Demo의x:" + Demo.x);
    Demo d = new Demo();
    System.out.println("Demo의 y:" +d.y);
//Demo클래스의 foo()와 bar()의 메소드호출
    Demo d2 = new Demo(); //생성자 호출
    d2.foo(); //non-static변수
//반환타입이 있는 statci 한 메소드 호출
    Strintg strVal = Demo.bar();
    System.out.println(strValue);

    //d와 d2는 서로다른 객체이다

    //인스턴스 변수 y 변경전
    System.out.println("d.y:"+d.y);
    System.out.println("d2.y:"+d2.y);


    //인스턴스 변수 y변경후 
    d2.y = 800;
    System.out.println("d.y:" +d.y);
    System.out.println("d2.y:" +d2.y);

    int xx; // 변수 선언은 error가 발생한다
    int xx = 0;//초기화를 해줘야함 
    //String타입의 전역변수를 초기화하지 않으면 null값으로 초기화된다
    System.out.println(v.str3); 

    }
}




```